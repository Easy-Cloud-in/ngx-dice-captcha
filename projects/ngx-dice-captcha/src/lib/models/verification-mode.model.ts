/**
 * Verification mode for the captcha
 */
export enum VerificationMode {
  /**
   * User must enter individual dice values using the overlay interface
   */
  INDIVIDUAL_DICE = 'INDIVIDUAL_DICE',
}
